1.concat
 - 2개의 배열을 합친다.
 - 2개의 문자를 합친다.

2.let_const
 - let, const문법 예제

3.slice
 - 배열의 특정값을 짤라내는 방법
 - 문자열 특정값 짤라내는 방법

4.split
 - 문자열에서 특정값($)으로 짤라 배열을 만들어준다.
 ex) ab$11$123asdd를 split하면 
	ab 11 123asdd가 된다.

5.replace
 - 문자열중에 특정값을 다른 값으로 바꾼다.

6.reverse
 - 배열의 순서를 뒤집는다.